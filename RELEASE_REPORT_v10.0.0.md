# Abyss Dash v10.0.0 发布报告

> 阶段 7 最终验收增补记录：2026-08-14
>
> 状态：部分验收；阶段 7 的真实墙钟已完成，但人工听音、运行时数组计数和若干响应式视觉风险仍未闭合。

## Git 基线与当前分支

- 工作目录：`D:\游戏设计\AbyssDash`
- 基线 HEAD：`138d07b`
- 当前分支：`codex/v10.0.0-layered-descent`
- 阶段 7 未暂存、未提交、未推送。
- 既有用户修改、未跟踪素材、references 和 workfiles 均保留。

## 代码改动摘要

- 统一页面运行时版本为 `10.0.0`，中文版本名为「层渊迁航」。
- 将区域、速度和 Boss 节点语义统一为深度；正式 Boss 击败后执行 300 / 800 / 1500 地图门闸转场。
- 转场期间清空危险物、释放输入并重置鱼群导演；演示 Boss 不切图、不改变正式进度。
- Boss 音乐在 `spawnBoss()` 成功建立 Boss 状态后立即且只启动一次，暂停、击败、重开和返回菜单生命周期保持隔离。

## 纳入的 24 个新运行资源

- 20 个背景 WebP：`assets/art/backgrounds/` 下四个区域各 5 张：浅海、沉船、海沟、黑暗深渊。
- 4 个障碍 WebP：`hazard-mine-pixel-v1.webp`、`cargo-crate-pixel-v2.webp`、`hazard-pillar-pixel-v1.webp`、`boundary-wall-pixel-v1.webp`。
- PNG 原稿、references 和 workfiles 不由运行时读取。

## 自动化验证

- Node：18 个测试，18 pass，0 fail。
- 主脚本解析：`SCRIPT_PARSE_OK`。
- 运行资源审计：`RUNTIME_ASSET_AUDIT_OK 29`。
- 资源尺寸审计：20 个背景为 `1672 × 941 RGB`，4 个障碍为 `RGBA`，通过。
- `git diff --check`：exit 0；仅有既有换行格式警告。

## 浏览器与地图转场

- 浏览器：Codex desktop in-app Browser。
- localhost：`http://127.0.0.1:8000/`。
- 阶段 7 尺寸矩阵：`844 × 390`、`390 × 844`、`1280 × 720`、`1920 × 1080`、`640 × 360`。
- 菜单、设置、HUD、暂停和 Boss 画面已在横屏尺寸实际检查；`390 × 844` 显示竖屏保护层 `ROTATE DEVICE`。
- 300：正式章鱼 Boss 击败后进入沉船区，关屏门闸和目标区域打开均已检查。
- 800：正式潜艇 Boss 击败后进入海沟，转场三态已检查。
- 1500：正式巨齿鲨 Boss 击败后进入黑暗区，转场三态已检查。
- 演示 Boss：结束后仍停留在原区域，未触发地图转场。
- 减少动态效果开启与关闭各完成一次 300 Boss 转场检查；开启时转场缩短并保持可读，关闭时保留完整动态转场。
- `640 × 360` 菜单帮助区底部出现裁切风险；设置面板可滚动。竖屏保护层覆盖游戏内容，暂停按钮仅在横屏单独验证。

## 资源请求与控制台

- 阶段 7 对 29 个运行资源执行 localhost HEAD 检查：`LOCALHOST_ASSET_HTTP_OK 29`。
- 阶段 7 浏览器控制台 warning/error 为空；未观察到脚本错误或音频 Promise 异常。

## 人工听音

- 未完成。自动化音频生命周期通过，但当前环境没有可执行的扬声器/耳机听音验收条件。

## 30 分钟墙钟测试

- 已实际保持浏览器运行约 30 分钟，未用 72,000 tick 模拟替代：
  - 0 分钟记录：约 31 秒，深度 300m，沉船区，PLAYING，warning/error 0。
  - 10 分钟记录：约 607 秒，深度 5582m，黑暗区，PLAYING，warning/error 0。
  - 20 分钟记录：约 1214 秒，深度 21839m，黑暗区，PLAYING，warning/error 0。
  - 30 分钟记录：约 1826 秒，深度 44655m，黑暗区，PLAYING，warning/error 0。
- `LONG_RUN_STABILITY_SIMULATION_OK` 与上述真实墙钟结果分开记录。
- 浏览器只读接口无法读取页面词法变量 `game`，因此 `fishes`、`particles`、`bubbles`、`obstacles`、`mines`、`jellyfishes` 六项数组计数未记录。
- 浏览器可见性切换后未观察到异常时间跳跃；未取得独立操作系统窗口失焦的完整证据。

## 未验证项与发布结论

- 人工听音仍未完成，当前环境没有可执行的扬声器/耳机听音验收条件。
- 六项运行时数组计数未记录；`640 × 360` 菜单帮助区裁切风险未修复；竖屏保护层下暂停按钮未单独观察。
- 当前为“部分验收”记录，不代表 v10.0.0 已完成最终发布验收。
