# Abyss Dash v9.7.0 基线检查报告

> 检查日期：2026-07-28  
> 检查分支：`agent/v9.8.0-deep-echoes`  
> 基线提交：`e303a80`  
> 对应计划：[`DEVELOPMENT_PLAN.md`](DEVELOPMENT_PLAN.md)

## 结论

v9.7.0 可以正常打开，主菜单、设置、鲨鱼彩蛋、上帝模式和三种 Boss 演示均能运行，运行素材加载成功，检查期间浏览器控制台没有错误。

阶段 1 同时确认了一个必须优先修复的流程问题：演示 Boss 仍会复用正式 Boss 的结算逻辑，可能污染正常进度、分数和成就。

## 已通过

- JavaScript 主脚本解析通过。
- 五个运行时素材路径全部存在：
  - 章鱼 Boss 序列帧。
  - 潜艇 Boss 序列帧。
  - 普通鲨鱼序列帧。
  - 赛博鲨鱼序列帧。
  - Boss 战音乐。
- 全新存档首次打开设置时，上帝模式选项保持隐藏。
- 连续点击鲨鱼五次后：
  - 鲨鱼播放冲刺动画。
  - 解锁 `Developer Tool (赛博黑客)` 成就。
  - 设置菜单保持关闭，没有自动跳转。
  - 上帝模式选项完成解锁。
- 上帝模式开启后，三个 Boss 选项可用；关闭后全部禁用，无敌同时关闭。
- 上帝模式、画质和减少动态效果设置刷新页面后仍能保留。
- 普通模式下按数字键不会召唤 Boss。
- 章鱼、潜艇和巨齿鲨均能通过设置预设并在开局后入场。
- 潜艇朝向玩家，鱼雷发射方向与朝向一致。
- 三种 Boss 演示期间未发现资源加载错误或控制台错误。
- 代码确认 Boss 音乐只在入场状态结束、切换为正式战斗状态后调用。

## 已确认问题

### P0：演示 Boss 会污染正式进度

当前演示召唤使用 `force: true` 创建 Boss，但 Boss 数据没有记录“演示”身份。Boss 结算时会无条件执行：

- 写入 `defeatedBosses`。
- 增加 Boss 奖励分数。
- 解锁对应的正式 Boss 成就。

影响：

- 先演示 Boss 后继续正常游戏，正式分数节点可能不再触发该 Boss。
- 玩家可以通过演示功能获得正式 Boss 成就和分数。

处理方向：

- 为 Boss 增加独立的 `isDemo` 状态。
- 演示结算不修改正式击败记录、分数和 Boss 成就。
- 演示结束后只恢复场景和音乐状态。

### P1：无操作演示时 Boss 容易贴近屏幕边缘

Boss 会持续跟随鲨鱼的纵向位置。开启无敌并不操作时，鲨鱼会上浮到顶部，三个 Boss 也会跟到顶部；由于实际精灵尺寸大于碰撞框，章鱼、潜艇和巨齿鲨都会出现不同程度的画面裁切。

处理方向：

- 根据 Boss 实际绘制尺寸限制纵向安全区。
- 演示模式下使用更稳定的跟随范围。
- 不改变正常战斗的追踪感。

### P1：数字键在菜单状态仍能预设 Boss

数字键监听会在上帝模式开启时调用 `requestGodBoss()`；该方法在菜单状态会保存待召唤 Boss。计划要求 `1 / 2 / 3` 只在游戏进行中生效，因此需要限制键盘入口。

保留行为：

- 设置菜单内的 Boss 按钮仍可用于开局前预设。

### P2：两条演示入口的场地清理不一致

游戏中按键召唤成功后会清空障碍、水雷和水母；菜单预设后开局召唤没有执行同一套场地准备逻辑。

处理方向：

- 提取统一的演示场地准备方法。
- 所有演示入口在 Boss 创建成功后执行同样的清理。

## 验证边界

以下项目不能由当前桌面自动化完整替代，保留为发布前人工验收：

- 触摸设备上的真实按住、松开和横竖屏切换。
- 从 0 分正常游玩到 300、800、1500 分的完整 Boss 流程。
- 对 Boss 音乐实际听感、叠加和淡出的人工听音检查。
- 连续 20 分钟的性能与内存耐久测试。

## 阶段 5 进展

2026-07-28 已完成自动化与桌面浏览器回归：

- 页面恢复浏览器缩放能力；设置等弹窗支持高缩放和触摸滚动。
- 主菜单、暂停、关闭、标签页和动态生成卡片改为原生可聚焦控件，并补充对话框、标签页和游戏画布语义。
- `Escape` 暂停/恢复、`Space` 下潜、暂停按钮和焦点返回已在浏览器验证。
- 页面失焦、`touchcancel` 和 `pointercancel` 会释放持续输入；游戏中失焦会暂停。
- 高画质、平衡与性能优先切换会立即修剪粒子、菜单气泡和背景物；减少动态效果开关即时生效。
- 新增 `tests/accessibility-performance.test.js` 和 `tests/long-run-stability.test.js`。
- 20 分钟等效模拟执行 72,000 个固定更新，验证粒子、菜单气泡、鱼群、背景物和浮动文字保持有界。

实体触摸操作、真实浏览器高倍率缩放、20 分钟墙钟耐久和 Boss 音乐听感仍需人工验收；等效模拟不代表这些项目已通过。

## 下一步

v9.8.0 已进入发布阶段并完成资源、README、版本号、GitHub 主分支和线上页面统一。实体触摸、高倍率缩放、20 分钟墙钟耐久和人工听音继续作为发布后人工验收。

## 修复追踪

2026-07-28 已在 `agent/v9.8.0-deep-echoes` 完成第一组修复：

- 演示 Boss 使用独立 `isDemo` 状态。
- 演示结算不再修改分数、正式击败记录和 Boss 成就。
- 正式 Boss 的奖励与成就逻辑通过对照测试。
- 数字键只在游戏进行中召唤 Boss。
- 菜单预设和游戏内召唤共用同一套场地准备逻辑。
- Boss 纵向追踪增加安全边界，章鱼、潜艇和巨齿鲨无操作演示均不再贴顶裁切。
- 新增 `tests/boss-demo-isolation.test.js` 防止演示与正式结算再次混用。
- Boss 音乐停止逻辑区分“正常击败后恢复环境音”和“游戏结束后保持静音”。
- 暂停发生在 Boss 音乐淡出期间时，环境音会延迟到恢复游戏后播放。
- 快速返回菜单会立即清理 Boss 音乐、淡出计时和暂停恢复标记。
- 新增 `tests/audio-lifecycle.test.js` 覆盖游戏结束、正常击败和淡出中暂停。
- 浏览器验证场上 Boss 防叠加、暂停恢复和快速刷新后再次召唤，控制台无错误。
- 鲨鱼彩蛋改为限时连续五次触发，过长间隔和超出总窗口均会重置计数。
- 鲨鱼图标增加键盘焦点与 `Enter`、空格键操作，首次解锁和重复冲刺使用同一条入口。
- 上帝模式只保留解锁资格；刷新后上帝模式、无敌和 Boss 召唤状态均恢复关闭。
- 重复触发彩蛋只重播冲刺动画，不重复写入或弹出隐藏成就。
- 新增 `tests/easter-egg-god-mode.test.js` 覆盖连续触发窗口、自动复位和键盘入口。
- 浏览器验证彩蛋限时窗口、设置不自动打开、刷新后安全复位和键盘触发，控制台无错误。
- 章鱼墨汁攻击增加提前 30 tick 的收缩眼环与短虚线提示，常驻绘制不使用光晕。
- 潜艇鱼雷增加提前 28 tick 的舰首瞄准线；鱼雷向左飞行，右侧尾焰与朝向一致。
- 潜艇三帧素材透明边界完整，主体中心偏差小于 1 像素，并加入逐帧亚像素对齐。
- 巨齿鲨改用固定的普通鲨鱼序列帧来源，不再依赖玩家当前皮肤；红色光晕只保留在最终 Boss。
- 巨齿鲨声波增加提前 30 tick 的轮廓提示，冲锋使用贯穿战斗路线的虚线预警。
- 减少动态效果时，巨齿鲨使用固定帧、较小光晕和静态冲锋线；性能模式下预警仍保持清晰。
- 新增 `tests/boss-readability.test.js` 覆盖三种攻击的“先预警、后发射”、鱼雷方向和视觉约束。
- 浏览器分别验证章鱼高画质、潜艇性能模式、巨齿鲨高画质与减少动态效果画面，控制台无错误。
