# 🦈 深渊冲刺 (Abyss Dash)

> 一款基于 HTML5 原生 Canvas 的反重力深海跑酷游戏。
> **单文件、零依赖、纯原生、开箱即玩。**

### 🎮 在线试玩 (Live Demo)
* 🌐 **官方主站 (GitHub Pages)：** https://guofudamo2007-lab.github.io/AbyssDash/
* 🚀 **备用节点 (Netlify)：** https://cheerful-bienenstitch-28078d.netlify.app/
## 🌊 核心特色玩法

* **反直觉物理操作：** 摒弃传统的“按键跳跃”，采用“按住下潜，松开上浮”的深海浮力模拟，带来极具新鲜感的下潜体验。
* **极致爽感与心流：** 吞噬发光小鱼积攒狩猎值，满怒即可触发【猩红冲刺】——无敌霸体+撞碎一切暗礁。
* **动态生态交互：** 带有加速增益和分数翻倍的“霓虹洋流”，以及触碰会导致操作极度迟缓的“麻痹水母群”，策略与反应并重。

## 🛠️ 技术亮点与工程实践

本项目不依赖任何第三方游戏引擎（如 Cocos/Egret）或前端框架，从零手写物理、渲染与状态控制，重点攻坚了以下核心问题：

| 工程痛点 | 产生的现象 | 解决方案与架构 |
| :--- | :--- | :--- |
| **刷新率依赖** | 144Hz/240Hz 屏幕下游戏运行速度是 60Hz 的数倍，导致高刷屏玩家必定暴毙。 | **工业级时间步长 (Fixed Time Step)：** 引入时间累加器（Accumulator），强制逻辑帧锁定 120Hz 更新，彻底剥离物理逻辑与渲染帧率。 |
| **数值崩坏** | 后期障碍物密不透风，且叠加洋流加速后画面完全失控。 | **平滑动态难度 (Dynamic Scaling)：** 将基础速度（线性增长）与障碍生成频率（对数衰减）解耦，并引入**绝对速度硬上限 (Hard Cap)**。 |
| **状态机混乱** | 暂停后重开、死亡后复活容易出现 UI 残留或重力异常。 | **面向对象重构与 FSM：** 重构为严格的 Class 架构（Renderer/AudioManager/GameEngine），实现规范的生命周期状态机控制。 |
| **移动端误触** | 手机端频繁触发“双击放大”、“下拉刷新”及分辨率错位。 | **全端自适应控制：** 物理宽高与 Canvas CSS 分离。彻底拦截 `touchstart`、`touch-action: none` 阻断浏览器默认手势。 |
| **高分屏画质发虚** | 在 Mac Retina 屏幕或高配手机上，Canvas 边缘出现明显锯齿和模糊。 | **逻辑与物理分辨率解耦：** 引入 `devicePixelRatio` 动态计算屏幕像素比，利用 `ctx.scale` 重新映射绘制系，实现像素级锐利。 |

## 💻 技术栈

* **渲染层：** HTML5 `<canvas>` 2D API 纯代码绘制特效（残影、发光、粒子）。
* **音频引擎：** Web Audio API（基于代码手搓合成器底层，生成三角波/锯齿波音效，**实现了无需外部 .mp3 文件的独立音效系统**）。
* **数据持久化：** 基于 `localStorage` 的轻量级本地最高分排行榜。

## 📂 如何运行

**方案一：在线体验 (双节点支持)**
* 🌐 主站：[GitHub Pages 部署版](https://guofudamo2007-lab.github.io/AbyssDash/)
* 🚀 备用：[Netlify 部署版](https://cheerful-bienenstitch-28078d.netlify.app/)

**方案二：本地运行**
1. 克隆本项目或直接下载 `index.html`。
2. 无需 Node.js 或构建工具（Webpack/Vite），直接双击用任意现代浏览器打开 `index.html` 即可运行。

## 🔄 迭代日志

* `v3.5` (Retina 高清重制版)：引入 `window.devicePixelRatio`，物理分辨率与逻辑分辨率彻底解耦，适配视网膜高分屏，实现像素级锐利画质。
* `v3.3 - v3.4` (稳定版)：完善移动端 UI 与新手引导，修复暂停状态生命周期与数据视图同步 Bug。
  *依赖：v3.0 的 FSM 架构 + v3.1 的时间步长*
* `v3.2`：重构数值策划，引入速度硬上限与平滑生成曲线。
  *依赖：v3.0 的 OOP 架构，便于独立调整参数*
* `v3.1`：重写主循环，引入固定时间步长（Fixed Time Step）解决帧率差异。
  *依赖：v3.0 的 GameEngine 类隔离*
* `v3.0`：底层代码 OOP（面向对象）完全重构。引入 Class 架构与生命周期状态机。
  *基础：此前为过程式代码，无法支撑后续优化*
## 🚧 已知问题与方向

* 长期运行后数组膨胀（对象池待引入）
* 无障碍支持（屏幕阅读器适配）
* 多语言本地化
