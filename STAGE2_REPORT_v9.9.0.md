# Abyss Dash v9.9.0 阶段 2 报告

> 完成日期：2026-07-29  
> 工作分支：`agent/v9.9.0-abyss-voyage`  
> 阶段：触摸、横竖屏与高缩放适配

## 结论

阶段 2 已完成。触摸释放、竖屏暂停、安全区域、粗指针触控目标、弹窗滚动、
焦点循环和高倍率布局已完成代码修复与本地浏览器检查。检查过程中发现并修复
图鉴标签在 200% 等效布局视口下被截断，以及游戏缺少 Boss 血条两个问题。

## 本阶段改动

- 触摸从游戏区开始、在按钮或弹窗上结束时，现在仍会优先释放下潜。
- `touchcancel`、`pointercancel`、窗口失焦、竖屏切换和尺寸变化都会释放输入。
- 游戏进行中转为手机竖屏会自动暂停；恢复横屏后由玩家主动继续。
- 主 HUD、教程、菜单角落按钮、弹窗和竖屏提示加入安全区域内边距。
- 粗指针设备上的关键按钮、范围输入和复选框使用至少 44px 的触控目标。
- 主菜单、弹窗和升级界面在低高度下可以滚动，不会卡死在不可见按钮上。
- 六个弹窗统一维护 `aria-hidden`，`Tab` 焦点限制在当前弹窗内。
- 设置、图鉴、契约、皮肤、成就关闭后返回原按钮；升级选择后返回 Canvas。
- 图鉴五个分类标签在高缩放时可以横向滑动，并自动滚动到键盘焦点。
- 新增 Canvas Boss 血条：宽度受限、颜色按 Boss 区分，演示 Boss 明确标注。
- 新增 `tests/responsive-input.test.js`，并扩展无障碍测试。

## 浏览器检查矩阵

| 检查环境 | 结果 |
| --- | --- |
| `844 × 390` 横屏 | 游戏容器为 `702 × 390` 并居中；菜单角落按钮完整；设置弹窗完整且可滚动 |
| `390 × 844` 竖屏 | 竖屏提示完整显示；游戏容器隐藏；下潜释放；暂停按钮状态变为“继续游戏” |
| `1280 × 720`、DPR 1.5 | Canvas 像素尺寸为 `1350 × 750`，与画质上限计算一致 |
| `1920 × 1080` | 游戏容器保持 9:5，设置弹窗在视口内 |
| 125% 等效布局 `1024 × 576` | 设置与图鉴完整、可滚动 |
| 150% 等效布局 `853 × 480` | 设置与图鉴完整、可滚动 |
| 200% 等效布局 `640 × 360` | 设置、图鉴、升级三选一、Boss 血条和左右 HUD 均可用 |

200% 检查中，图鉴标签栏的可视宽度为 527px、内容宽度为 660px；修复后可
横向滚动，选择 Boss 分类时标签会滚动进入可视区域。升级三选一生成三张卡片，
全部位于横向可视范围内，纵向内容由弹窗滚动承载。

## 自动化结果

- `ACCESSIBILITY_PERFORMANCE_OK`
- `AUDIO_LIFECYCLE_OK`
- `BOSS_DEMO_ISOLATION_OK`
- `BOSS_READABILITY_OK`
- `EASTER_EGG_GOD_MODE_OK`
- `LONG_RUN_STABILITY_SIMULATION_OK`
- `NORMAL_PROGRESSION_OK`
- `RESPONSIVE_INPUT_OK`
- `SETTINGS_PERSISTENCE_OK`
- `SCRIPT_PARSE_OK`
- `RUNTIME_ASSET_AUDIT_OK 5`
- 本地浏览器控制台：0 条记录

## 验证边界

浏览器尺寸检查使用浏览器的响应式视口覆盖；125%—200% 使用与
`1280 × 720` 浏览器缩放后相同的有效 CSS 布局视口。DPR 1.5 则在恢复真实
桌面视口后单独验证。

桌面浏览器无法证明实体触屏的触摸采样、刘海真实安全区域或系统手势冲突。
这些项目仍需在实体横屏触摸设备上人工验收。临时升级弹窗测试入口已在检查后
移除，不会进入提交。

## 下一步

进入阶段 3：局后统计、近失反馈与节奏。
