# Abyss Dash v9.8.0「深渊回响」开发计划

> 历史归档：本文记录 v9.8.0 当时的实现与验收口径。当前 v9.9.0 计划见
> [`DEVELOPMENT_PLAN_v9.9.0.md`](DEVELOPMENT_PLAN_v9.9.0.md)；Boss 音频顺序已调整为“音乐先起奏，再开始 Boss 入场动画”。
>
> 计划状态：已完成（阶段 1—6 已于 2026-07-28 完成；实体触屏与墙钟耐久继续作为发布后人工验收）  
> 基线版本：v9.7.0「深渊统御」  
> 目标版本：v9.8.0「深渊回响」

## 一、迭代目标

本轮不重做现有核心玩法，重点把 v9.7.0 已完成的 Boss、美术资源和上帝模式打磨成稳定、清晰、可长期维护的正式版本。

主要目标：

1. 让演示召唤与正常游戏流程完全隔离。
2. 统一 Boss 入场、战斗、退场和音乐的状态管理。
3. 完善鲨鱼彩蛋、上帝模式和隐藏成就的反馈。
4. 保持章鱼与潜艇效果克制，只让最终 Boss 巨齿鲨拥有强烈光效。
5. 完成桌面端、触摸端、不同画质和减少动态效果模式的完整回归。

## 二、本轮不做

- 不移除 Roguelite 突变、四区域、皮肤、成就和契约系统。
- 不改变“按住下潜、松开上浮”的核心操作。
- 不增加新的普通操作按键。
- 不再次更换章鱼和潜艇的主体美术，除非测试发现方向、透明边缘或帧切换问题。
- 不把原始视频和制作中间稿加入游戏运行时加载链。

## 三、实施阶段

### 阶段 1：建立 v9.7.0 稳定基线（已完成，2026-07-28）

> 自动化基线完成于 2026-07-28，结果见 [`BASELINE_REPORT_v9.7.0.md`](BASELINE_REPORT_v9.7.0.md)。触摸实机、正常分数流程和 20 分钟耐久测试保留到阶段 5。

- 从主菜单完成一次正常开局。
- 分别验证鼠标、空格和触摸下潜。
- 检查教程、设置、存档和皮肤读取。
- 正常推进到三个 Boss 分数节点，记录入场、音乐、补给和退场状态。
- 使用上帝模式分别召唤三个 Boss，记录与正常流程的差异。
- 检查浏览器控制台、资源加载和本地存储。

完成标准：

- 形成一份明确的问题清单。
- 区分代码缺陷、素材缺陷和仅需视觉微调的问题。
- 没有问题时不修改对应系统。

### 阶段 2：重构 Boss 演示状态（已完成，2026-07-28）

> 阶段状态：代码与浏览器回归已完成。演示身份隔离、数字键作用域、统一场地准备、Boss 纵向安全区、音乐退出、暂停恢复与连续召唤均已验证；人工听音检查保留到阶段 5。

为 Boss 召唤建立明确的演示流程：

```text
等待召唤
→ 校验上帝模式
→ 清理冲突实体
→ Boss 入场
→ 正式开战
→ 播放 Boss 音乐
→ Boss 被击败或手动结束
→ 停止音乐并恢复普通流程
```

具体要求：

- `1`、`2`、`3` 只在上帝模式开启且游戏进行中生效。
- Boss 已存在时拒绝重复召唤，并给出简短提示。
- 按键时只创建召唤请求，不提前播放 Boss 音乐。
- 音乐只能在 Boss 正式进入战斗状态时播放一次。
- Boss 结束、重新开始、返回菜单或页面失焦时正确停止或淡出音乐。
- 演示召唤不修改正常流程的 Boss 击败记录、分数节点和成就条件。
- 演示结束后，普通鱼群、障碍和区域节奏能够恢复。

完成标准：

- 连续测试三个 Boss，不出现音乐叠加、提前播放或结束后残留。
- 演示流程不会跳过或污染正常 Boss 进度。

### 阶段 3：完善鲨鱼彩蛋与上帝模式（已完成，2026-07-28）

- 在 2.2 秒总窗口内、相邻操作不超过 650 毫秒，连续触发鲨鱼五次后播放一次清晰但不过度的冲刺动画。
- 鲨鱼支持鼠标点击、`Enter` 和空格键触发。
- 首次触发时解锁隐藏成就，重复触发不重复写入。
- 彩蛋只解锁设置选项，不自动打开设置或改变当前页面。
- 设置中增加“上帝模式”总开关和“无敌”子开关。
- 关闭上帝模式时立即停用 Boss 召唤键和无敌效果。
- 在设置项旁标明 `1 / 2 / 3` 对应的 Boss。
- 刷新页面后保留解锁状态，但上帝模式与无敌均恢复为关闭，避免意外进入演示状态。

完成标准：

- 普通玩家不会误触演示功能。
- 已解锁玩家能够从设置中明确找到并控制功能。
- 彩蛋动画、成就提示和设置出现的顺序一致。

### 阶段 4：Boss 视觉与战斗可读性（已完成，2026-07-28）

#### 章鱼

- 保留呼吸、轻微悬浮、触手摆动和气泡。
- 限制常驻光晕尺寸、透明度和模糊半径。
- 墨汁攻击前使用收缩眼环和短距离虚线提示，不增加常驻光晕。

#### 潜艇

- 保持从右侧进入并朝向玩家。
- 三帧巡航素材透明边界完整，使用逐帧亚像素偏移消除主体中心轻微跳位。
- 发射前显示舰首瞄准线；鱼雷向左飞行，尾焰位于右侧。

#### 巨齿鲨

- 保留最终 Boss 专属的强烈光效和压迫感。
- 强光不能遮挡玩家、攻击预警和碰撞范围。
- 减少动态效果模式下保留轮廓与攻击提示，削弱闪光和屏幕震动。
- 使用固定的巨齿鲨序列帧来源，不再跟随玩家当前皮肤变化。

完成标准：

- 三个 Boss 仅看轮廓、动作和特效即可区分。
- 高画质和性能优先模式下，攻击提示都能看清。

### 阶段 5：性能、触摸与无障碍回归（自动化与桌面浏览器回归完成，2026-07-28）

> 已完成：三档画质切换、减少动态效果开关、可缩放视口、设置面板滚动、键盘焦点、暂停按钮、`Escape` 暂停/恢复、`Space` 下潜、失焦释放输入代码路径、触摸取消释放输入代码路径，以及 72,000 个固定更新（60 Hz 下 20 分钟等效）的有界资源模拟。桌面浏览器控制台无错误。
>
> 人工验收保留：真实触摸设备的按住/松开与横竖屏切换、真实浏览器高倍率缩放、20 分钟墙钟游玩及人工听音。自动化模拟用于提前发现资源数组无界增长，不替代实体设备和墙钟耐久测试。

- 高画质、平衡和性能优先各完成一轮 Boss 测试。
- 减少动态效果开启和关闭各完成一轮测试。
- 验证桌面横屏、触摸横屏和竖屏提示。
- 连续游玩至少 20 分钟，检查卡顿、音频残留和内存增长。
- 页面失焦、暂停、恢复和重新开始时不发生时间跳跃。
- 检查高分辨率和浏览器缩放下的设置面板与 Boss 血条。

完成标准：

- 控制台无错误。
- 所有运行素材请求成功。
- 长时间游玩没有明显性能退化。
- 触摸操作不与上帝模式设置冲突。

### 阶段 6：资源与发布整理（已完成，2026-07-28）

> 运行资源、参考素材、制作中间稿和归档已分层保留；旧的令牌上传脚本已删除。页面、README 与资源说明统一为 v9.8.0，并完成自动化、语法、资源路径、本地 HTTP 浏览器、GitHub `main` 和 GitHub Pages 检查。

- 运行素材继续放在 `assets/art/characters`、`assets/art/bosses` 和 `assets/audio/music`。
- 原始视频、抽帧和中间稿保留在 references、workfiles 和 archive，不由游戏直接加载。
- 删除确认无引用的重复运行素材，但保留有回退价值的归档文件。
- 更新 `README.md`、`assets/README.md` 和 `QA_CHECKLIST.md`。
- 将页面版本、菜单版本和 README 版本统一更新为 v9.8.0。
- 完成语法检查、资源路径检查和浏览器实际运行检查。
- 提交到 GitHub，合并到 `main`，再确认 GitHub Pages 部署结果。

完成标准：

- 本地版本、GitHub 主分支和线上页面版本一致。
- 发布说明明确列出功能变化、修复项和验证范围。

## 四、验收清单

### 正常游戏

- [x] 不开启上帝模式时，`1`、`2`、`3` 不影响游戏。
- [ ] 三个 Boss 在 300、800、1500 分正常触发。
- [x] Boss 音乐只在正式开战后播放。
- [x] Boss 结束后音乐和场景状态正确恢复。

### 演示模式

- [x] 连点鲨鱼触发冲刺、成就和设置解锁，不自动跳转。
- [x] 上帝模式和无敌可以分别开关。
- [x] `1`、`2`、`3` 正确召唤章鱼、潜艇和巨齿鲨。
- [x] 重复召唤和 Boss 冲突有明确提示。
- [x] 演示召唤不污染正常进度和成就。

### 美术与音频

- [x] 章鱼光效克制，动画保持生命感。
- [x] 潜艇方向、鱼雷方向和序列帧一致。
- [x] 巨齿鲨保留最终 Boss 专属强光效。
- [x] 减少动态效果模式下没有强闪烁和过度震动。

### 发布

- [x] JavaScript 语法检查通过。
- [x] 所有运行时资源路径存在。
- [x] 浏览器控制台无错误。
- [x] README、版本号和发布说明已更新。
- [x] GitHub `main` 与线上页面显示 v9.8.0。

## 五、提交顺序

建议按以下范围分别提交，方便回退：

1. `test: record v9.7.0 regression baseline`
2. `fix: isolate boss demo summon state`
3. `fix: synchronize boss music lifecycle`
4. `polish: refine god mode and shark easter egg`
5. `polish: improve boss readability and restrained effects`
6. `docs: update v9.8.0 release and QA notes`
7. `release: Abyss Dash v9.8.0`

## 六、完成定义

只有同时满足以下条件，本轮才算完成：

- 正常流程和演示流程均可独立完成三个 Boss。
- 没有提前播放、重复播放或残留的 Boss 音乐。
- 上帝模式不会被普通操作误触。
- 章鱼、潜艇和巨齿鲨的视觉层级符合设计要求。
- 桌面、触摸、画质和减少动态效果模式均通过回归。
- GitHub 主分支与线上版本完成更新。
