# Abyss Dash v9.9.0「深渊长航」开发计划

> 计划状态：执行中（阶段 0—2 已完成，阶段 3 进行中）
> 基线版本：v9.8.0「深渊回响」  
> 目标版本：v9.9.0「深渊长航」  
> 核心原则：保留现有跑酷、Roguelite、契约、成就、三 Boss 与上帝模式，优先补齐完整长局体验，不进行大规模玩法重做。

## 一、迭代目标

1. 不依赖上帝模式，验证从深度 0 到 1500 最终 Boss 的完整正常流程。
2. 完成横屏触摸、竖屏提示、高 DPI 和浏览器高倍率缩放适配。
3. 增加清晰的局后统计与近失反馈，让玩家理解每次失败和成长。
4. 统一环境音、Boss 音乐与音效的混音和暂停恢复行为。
5. 完成 30 分钟墙钟耐久测试，并建立可重复的发布验收流程。

## 二、本轮边界

本轮包含：

- 正常深度流程、Boss 节点和重新开始回归。
- 触摸、响应式布局、焦点和高缩放修复。
- 局后统计、近失奖励显示和局内反馈调整。
- 音频混音与生命周期完善。
- 性能、资源上限、长期运行和发布整理。

本轮不包含：

- 新增第四名 Boss。
- 更换现有核心美术风格。
- 联网排行榜、账号系统或云存档。
- 破坏现有 `localStorage` 存档格式的重构。
- 将单文件运行方式改成需要打包工具的工程。

## 三、执行阶段

### 阶段 0：建立 v9.9.0 分支与发布基线（已完成，2026-07-29）

> 已从 GitHub `main` 的 v9.8.0 合并提交 `325854f` 创建
> `agent/v9.9.0-abyss-voyage`。六项 Node 测试、主脚本语法、五个运行资源
> 路径和本地 HTTP 浏览器基线全部通过，控制台无警告或错误。旧目录
> `D:\AbyssDash` 经确认为空后已删除并复查为不存在。详细结果见
> [`BASELINE_REPORT_v9.8.0.md`](BASELINE_REPORT_v9.8.0.md)。

- 从 GitHub `main` 的 v9.8.0 合并提交建立 `agent/v9.9.0-abyss-voyage`。
- 重新运行六项现有 Node 测试、主脚本语法检查和运行资源审计。
- 使用本地 HTTP 打开菜单、设置并开始一次游戏，记录控制台基线。
- 重新检查旧目录 `D:\AbyssDash`；只有确认没有进程占用且不包含文件时才删除。

完成标准：

- 新分支只包含 v9.9.0 工作。
- v9.8.0 基线测试全部通过。
- 旧目录状态有当前检查结果，不沿用旧结论。

### 阶段 1：正常长局与 Boss 节点回归（已完成，2026-07-29）

> 梳理确认正式 Boss 与四区切换共用深度进度，而不是得分：深度 300、800、
> 1500 依次触发章鱼、潜艇和巨齿鲨。为保持现有跑酷节奏，本轮没有改成得分
> 触发，而是统一代码命名、README 和测试语义。新增正常进度、重开清理、
> 教程完成、设置持久化、存档重置和源文件完整性测试；九项 Node 测试全部
> 通过。详细结果见 [`STAGE1_REPORT_v9.9.0.md`](STAGE1_REPORT_v9.9.0.md)。

- 为深度 300、800、1500 三个正常 Boss 节点建立确定性测试。
- 验证正常 Boss 与演示 Boss 的分数、击败记录、成就和奖励完全独立。
- 覆盖 Boss 入场、补给、紧急护盾、击败、环境恢复和下一个 Boss 解锁。
- 验证死亡、重新开始、返回菜单和快速重开不会保留旧 Boss、投射物或音频状态。
- 增加设置持久化与“重置本地进度”回归测试。
- 新增 `tests/normal-progression.test.js` 和 `tests/settings-persistence.test.js`。
- 新增 `tests/source-integrity.test.js`，将主脚本解析与五项运行资源检查纳入固定回归。

完成标准：

- 不开启上帝模式也能按节点触发三名 Boss。
- 完整流程不会污染下一局。
- 自动化测试覆盖正常触发和演示触发两条路径。

### 阶段 2：触摸、横竖屏与高缩放适配（已完成，2026-07-29）

> 修复触摸在控件上结束时未释放下潜、转入竖屏后仍继续运行、弹窗缺少
> 安全区域边距和焦点循环的问题。新增统一响应式弹窗容器、粗指针 44px
> 触控目标、图鉴标签横向滚动和 Canvas Boss 血条。完成 `844 × 390`、
> `390 × 844`、`1280 × 720`、`1920 × 1080` 与 125%—200% 等效布局
> 视口检查，浏览器控制台无错误。详细结果见
> [`STAGE2_REPORT_v9.9.0.md`](STAGE2_REPORT_v9.9.0.md)。

- 验证触摸按住、松开、`touchcancel`、`pointercancel` 和失焦释放。
- 防止暂停按钮、设置控件和上帝模式选项误触发下潜。
- 适配 `844 × 390` 横屏触摸、`390 × 844` 竖屏提示、桌面 1280 × 720 与 1920 × 1080。
- 检查 125%、150% 和 200% 浏览器缩放下的设置、图鉴、升级三选一和 Boss 血条。
- 补充安全区域内边距，避免刘海、圆角屏和系统手势遮挡。
- 完善弹窗焦点约束和关闭后的焦点返回。

完成标准：

- 横屏触摸可完整游玩，竖屏提示不会遮挡或卡死页面。
- 高缩放下所有弹窗可滚动，关键按钮可见。
- 触摸操作不与暂停、设置和上帝模式冲突。

### 阶段 3：局后统计、近失反馈与节奏（进行中，2026-07-29）

> 已先处理玩家实测发现的后期冲刺鱼群断档：冲刺时鱼群生成间隔最多缩短至
> 48 个逻辑帧，黑暗区每四轮生成一次两条鱼的小群；普通速度下的四区间隔和
> 巨齿鲨战 40 帧专用补给保持不变。确定性测试已覆盖屏幕可见寿命、Boss 隔离
> 与 30 条鱼资源上限。局后统计和近失反馈仍按本阶段计划继续。

- 在游戏结束界面增加本局统计：
  - 最终得分与深度。
  - 吞鱼数量。
  - 最高连击。
  - 近失次数。
  - 击败 Boss 数量。
  - 获得的突变数量。
- 修复近失奖励只加分、不显示 `CLOSE CALL +3` 的反馈缺口。
- 让近失、护盾抵挡、升级保护和 Boss 击败使用不同但克制的提示样式。
- 检查 0—300、300—800、800—1500 三段节奏，减少无事件空档和连续伤害峰值。
- 不增加高频警告音，危险提示继续以视觉为主。

完成标准：

- 玩家能从结算页理解本局表现和失败原因。
- 近失奖励可见但不会遮挡战斗。
- 三段流程难度递进清晰，没有突然失控的生成密度。

### 阶段 4：音频混音与生命周期

- 统一环境音、Boss 音乐和音效音量层级。
- Boss 入场、正式开战、击败和返回环境音使用一致淡入淡出。
- 页面失焦、暂停和切换标签页时冻结音频状态；恢复时不叠加。
- 限制连续吞鱼、碰撞和攻击音效的高频叠加。
- 在扬声器和耳机环境各进行一次人工听音。
- 扩展 `tests/audio-lifecycle.test.js`，覆盖长局与快速连续状态切换。

完成标准：

- 没有提前播放、双重播放、爆音或残留音乐。
- 音效能反馈操作，但不会压过 Boss 音乐。
- 减少动态效果不会改变必要的音频状态逻辑。

### 阶段 5：性能与 30 分钟耐久

- 高画质、平衡和性能优先各完成 Boss 与大量粒子压力测试。
- 减少动态效果开启和关闭各完成一轮长局。
- 连续运行 30 分钟，在 0、10、20、30 分钟记录：
  - 帧率体感。
  - 粒子、气泡、投射物与背景物数量。
  - 浏览器内存趋势。
  - 控制台警告与错误。
  - 音频是否残留。
- 验证页面失焦后不会通过固定更新补跑造成时间跳跃。
- 将墙钟耐久结果与 72,000 更新等效模拟分开记录。

完成标准：

- 30 分钟内无明显卡顿恶化、无界数组增长或音频叠加。
- 三档画质资源上限真实生效。
- 自动化模拟和墙钟测试的验证边界写清楚。

### 阶段 6：v9.9.0 发布

- 更新页面标题、菜单版本、README、资源说明和 QA 清单。
- 新增 v9.9.0 版本记录，明确功能变化、修复项和人工验收范围。
- 运行全部 Node 测试、语法检查、资源路径检查和本地 HTTP 浏览器检查。
- 提交到独立发布分支，通过 PR 合并到 GitHub `main`。
- 等待 GitHub Pages 部署成功，确认线上页面版本与资源请求。

完成标准：

- 本地、GitHub `main` 和 GitHub Pages 均显示 v9.9.0。
- 发布提交、PR、合并提交和 Pages 部署均有可追踪记录。

## 四、验收矩阵

### 正常流程

- [ ] 从深度 0 正常触发深度 300、800、1500 Boss。
- [ ] 三名 Boss 均可正常击败，补给与紧急护盾有效。
- [ ] 正常流程的分数、成就和击败记录正确保存。
- [ ] 死亡、重开和返回菜单不会继承上一局战斗状态。

### 触摸与界面

- [ ] 横屏触摸按住与松开稳定。
- [ ] 竖屏提示出现且可恢复到横屏。
- [ ] 125%、150%、200% 缩放下弹窗和 Boss 血条完整。
- [ ] 暂停、设置和上帝模式控件不会误触发下潜。

### 新反馈

- [ ] 局后统计显示正确。
- [ ] `CLOSE CALL +3` 可见且不遮挡战斗。
- [ ] 护盾、保护时间、近失和 Boss 击败提示可区分。

### 音频与稳定性

- [ ] Boss 音乐只在正式开战后播放。
- [ ] 暂停、失焦、重开和返回菜单无音频叠加。
- [ ] 扬声器与耳机听音均无明显爆音或音量失衡。
- [ ] 30 分钟墙钟耐久无明显性能退化。

### 发布

- [ ] 全部自动化与语法检查通过。
- [ ] 运行资源请求成功，浏览器控制台无错误。
- [ ] README、QA、版本号和发布说明统一为 v9.9.0。
- [ ] GitHub `main` 与 GitHub Pages 显示 v9.9.0。

## 五、建议提交顺序

1. `test: establish v9.9.0 normal-run baseline`
2. `fix: harden normal boss progression and restart state`
3. `polish: improve touch layout and high-zoom accessibility`
4. `feat: add run summary and near-miss feedback`
5. `polish: balance audio mix and lifecycle`
6. `test: record 30-minute endurance results`
7. `release: Abyss Dash v9.9.0`

## 六、执行规则

- 每完成一个阶段，先运行对应测试并记录结果，再进入下一阶段。
- 自动化、桌面浏览器、实体触屏和人工听音分别报告，不互相代替。
- 新反馈保持克制，不用巨大光晕和高频警告声掩盖操作信息。
- 如果发现现有存档需要迁移，先增加兼容读取，再修改写入格式。
- 发布前不删除 references、workfiles 或 archive 中具有回退价值的素材。
